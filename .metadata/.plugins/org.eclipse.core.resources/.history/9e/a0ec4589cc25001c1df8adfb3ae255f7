package task4;

public class TowerOfHanoi {

	static void towerOfHanoi(int disk, char source, char to_rod, char aux_rod) {
		if (disk == 1) {
			System.out.println("Move disk 1 from rod " + source + " to rod " + to_rod);
			return;
		}
		towerOfHanoi(disk - 1, source, aux_rod, to_rod);
		System.out.println("Move disk " + disk + " from rod " + source + " to rod " + to_rod);
		towerOfHanoi(disk - 1, aux_rod, to_rod, source);
	}

	// Driver code
	public static void main(String args[]) {
		int n = 4; // Number of disks
		towerOfHanoi(n, 'A', 'C', 'B'); // A, B and C are names of rods
	}
}

// This code is contributed by jyoti369
